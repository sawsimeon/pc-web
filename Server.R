library(shiny)


shinyServer(function(input, output, session) {
  
#  output$video <- renderUI({
#    tags$video(src="It_Gen_Ler_Mu.mp4", type = "video/mp4", autoplay = "autoplay", controls = "controls", width = "100%", height = "100%")
#  })
  
  
  })